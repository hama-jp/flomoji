# Copy this file to .env and fill in your keys before running local builds or E2E tests.
# PLAYWRIGHT_OPENAI_API_KEY is picked up by Playwright tests (e.g. copilot.spec.ts).
# OPENAI_API_KEY is a generic fallback used across dev tooling.
# VITE_OPENAI_API_KEY allows the web app to access the same key via import.meta.env.

PLAYWRIGHT_OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_API_KEY=sk-your-openai-api-key
VITE_OPENAI_API_KEY=sk-your-openai-api-key
